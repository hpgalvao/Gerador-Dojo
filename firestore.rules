rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Helper functions
    function isValidId(id) {
      return id is string && id.size() <= 128 && id.matches('^[a-zA-Z0-9_\\-]+$');
    }

    function incoming() {
      return request.resource.data;
    }

    // Default deny
    match /{document=**} {
      allow read, write: if false;
    }

    // Landing Pages
    match /landing_pages/{pageId} {
      allow read: if true; // Publicly readable
      allow write: if request.auth != null; // Only authenticated users (admins) can edit
    }

    // Leads
    match /leads/{leadId} {
      allow create: if true; // Anyone can submit a lead
      allow read, update, delete: if request.auth != null; // Only admins can see lead data
    }
  }
}
